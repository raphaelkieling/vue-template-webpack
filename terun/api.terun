import createMock from '../../utils/mock';

// TODO: Implementar
const mock = createMock(['id', 'model']);

export default {
    baseUrl: '/{{entidade | lowercase}}',
    getAll() {
        return mock.many(4);
    }
};
