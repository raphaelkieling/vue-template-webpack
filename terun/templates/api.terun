// Tempo de hoje: {{tempo}}

import createMock from '../../utils/mock';

const mock = createMock([
    {{#fields}}
    '{{name}}',
    {{/fields}}
]);

export default {
    baseUrl: '/{{entity | underscore | lowercase}}',
    getAll() {
        return mock.many();
    }
};
